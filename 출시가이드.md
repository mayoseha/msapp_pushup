# 푸쉬업 앱 — 빌드 및 구글플레이 출시 가이드

PC에 안드로이드 스튜디오를 설치하지 않고, 웹브라우저만으로 앱 파일을 만들어 출시하는 방법.

---

## 큰 그림

```
GitHub에 코드 올리기
      ↓
GitHub가 대신 빌드 (무료)
      ↓
AAB 파일 내려받기
      ↓
Play Console에 업로드
```

빌드는 GitHub Actions가 남의 컴퓨터에서 대신 돌려줍니다. 공개 저장소는 무료입니다.

---

## 1단계. GitHub에 코드 올리기

1. github.com 로그인 (계정이 없으면 가입)
2. 우측 상단 `+` → **New repository**
3. 이름: `pushup-app`, **Public** 선택 → Create repository
4. 만들어진 화면에서 **uploading an existing file** 클릭
5. 압축을 푼 폴더 안의 **내용물 전체**를 끌어다 놓기
   - 주의: `pushup` 폴더째로 올리면 안 됩니다. 폴더를 열고 안에 있는 것들을 올려야 합니다
   - `.github` 폴더가 안 올라가면 화면이 숨김 폴더를 거른 것이니, 그 폴더만 따로 다시 올립니다
6. **Commit changes** 클릭

---

## 2단계. 비밀번호 3개 저장하기

저장소 → **Settings** → 좌측 **Secrets and variables** → **Actions** → **New repository secret**

아래 두 개를 만듭니다. 비밀번호는 대표님이 정하되 **반드시 따로 적어두십시오.** 잃어버리면 앱 업데이트를 못 합니다.

| 이름 | 값 |
|---|---|
| `KEYSTORE_PASSWORD` | 직접 정한 비밀번호 (영문+숫자 8자 이상) |
| `KEY_PASSWORD` | 위와 같은 값을 넣어도 됩니다 |

---

## 3단계. 서명키 만들기 (딱 한 번)

앱에는 "이건 내가 만든 앱"이라는 도장이 필요합니다. 이걸 서명키라고 합니다.

1. 저장소 → **Actions** 탭
2. 왼쪽 목록에서 **1. 서명키 만들기** 클릭
3. 오른쪽 **Run workflow** 버튼 → 초록 버튼 한 번 더
4. 1분쯤 뒤 완료되면, 그 실행 결과 화면 맨 아래 **Artifacts** → `keystore` 내려받기
5. 압축을 풀면 `keystore-base64.txt` 가 있습니다. **메모장으로 열어 전체 내용을 복사**
6. 다시 Settings → Secrets → **New repository secret**
   - 이름: `KEYSTORE_BASE64`
   - 값: 방금 복사한 내용 붙여넣기
7. 내려받은 `upload.jks` 파일은 **구글 드라이브 등에 영구 보관**하십시오

> 이 파일과 비밀번호를 잃어버리면 같은 앱을 업데이트할 수 없습니다. 새 앱으로 다시 올려야 합니다.

---

## 4단계. 앱 빌드하기

1. **Actions** 탭 → **2. 앱 빌드** → **Run workflow**
2. 3~5분 기다립니다
3. 초록 체크가 뜨면 결과 화면 아래 **Artifacts** → `pushup-app` 내려받기
4. 압축 안에 두 개가 있습니다
   - `app-release.aab` — Play Console에 올릴 파일
   - `app-release.apk` — 폰에 바로 설치해서 테스트할 파일

**빨간 X가 뜨면** 실패한 단계를 눌러 로그를 펼치고, 빨간 글씨 부분을 캡처해서 저에게 주십시오. 대개 버전 숫자를 올리라는 메시지입니다.

---

## 5단계. 폰에서 먼저 테스트

Play Console에 올리기 전에 반드시 실기기 확인을 합니다.

1. `app-release.apk` 를 폰으로 전송 (카카오톡 나에게 보내기, 구글 드라이브 등)
2. 폰에서 파일 열기 → "출처를 알 수 없는 앱" 허용 → 설치
3. 확인할 것
   - [ ] 폰을 바닥에 놓고 손을 화면 위쪽에 댔다 떼면 1개씩 올라가는가
   - [ ] 손을 댄 채 가만히 있으면 안 올라가는가
   - [ ] 앱을 껐다 켜도 숫자가 유지되는가
   - [ ] 한국어 음성이 나오는가
   - [ ] 목표를 5로 바꾸고 5개 채우면 폭죽이 터지는가
   - [ ] 실제로 엎드려서 20개 해봤을 때 개수가 맞는가

여기서 문제가 있으면 코드를 고쳐 4단계부터 다시 합니다.

---

## 6단계. 개발자 계정 만들기 — 여기가 갈림길

**반드시 "조직" 계정으로 등록하십시오.**

play.google.com/console 접속 → 계정 유형 선택 화면에서 **개인이 아니라 조직**을 고릅니다.

이유: 2023년 11월 13일 이후 만든 **개인 계정**은 최근 14일 이상 계속 참여한 테스터 12명 이상으로 비공개 테스트를 마쳐야 프로덕션 출시를 신청할 수 있습니다. 조직 계정은 이 요건의 대상이 아닙니다.

- 등록비: 미화 25달러, 1회 납부
- 조직 계정은 사업자 확인 절차가 있습니다. 행운공인중개사 사업자등록증을 준비하십시오
- D-U-N-S 번호를 요구받을 수 있습니다. 무료 발급이며 며칠 걸립니다
- 확인에 며칠~2주가 걸릴 수 있으니 **가장 먼저 시작해 두십시오**

---

## 7단계. 미리 준비할 것

Play Console 작성 단계에서 반드시 필요합니다. 미리 만들어두면 하루 만에 끝납니다.

| 항목 | 내용 |
|---|---|
| 앱 이름 | 30자 이내 |
| 간단한 설명 | 80자 이내 |
| 자세한 설명 | 4000자 이내 |
| 앱 아이콘 | 512×512 PNG |
| 그래픽 이미지 | 1024×500 PNG |
| 스크린샷 | 폰 화면 캡처 최소 2장 |
| 개인정보처리방침 URL | 아래 참고 |

**개인정보처리방침 URL 만드는 법 (무료)**

같이 드린 `개인정보처리방침.md` 파일을 GitHub 저장소에 올린 뒤,
저장소 → Settings → Pages → Source를 `main` 으로 지정하면
`https://아이디.github.io/pushup-app/` 형태의 주소가 생깁니다. 이 주소를 넣으면 됩니다.

---

## 8단계. Play Console 업로드

1. **앱 만들기** → 이름, 언어, 앱/게임 구분, 무료/유료 선택
2. **테스트 및 출시 → 테스트 → 내부 테스트** 에서 먼저 `app-release.aab` 업로드
   - 본인 계정만 테스터로 넣고 실제 설치까지 확인
3. 문제없으면 **프로덕션** 으로 같은 파일 업로드
4. **정책 및 프로그램 → 앱 콘텐츠** 의 설문을 전부 채웁니다
   - 데이터 보안: **수집도 공유도 하지 않음** 으로 답합니다. 이 앱은 인터넷 권한이 없으므로 사실 그대로입니다
   - 광고 없음, 대상 연령층 성인
5. 심사 제출

**반려를 부르는 두 가지**
- 데이터 보안 답변이 앱의 실제 동작과 다른 경우
- 스토어 설명이 실제 기능보다 과장된 경우

이 앱은 인터넷도 안 쓰고 기능도 단순해서 둘 다 걸릴 일이 없습니다.

---

## 나중에 앱을 고칠 때

1. GitHub에서 해당 파일을 열고 연필 아이콘 → 수정 → Commit
2. `app/build.gradle.kts` 의 `versionCode` 를 1 올립니다 (필수)
3. Actions → 2. 앱 빌드 → Run workflow
4. 새 AAB를 Play Console에 업로드

---

## 확실하지 않은 부분

정직하게 남깁니다.

- **빌드 도구 버전** — 코드에 적어둔 Gradle, AGP, compileSdk 숫자는 시간이 지나면 낡습니다. 빌드가 실패하면 로그가 어떤 버전을 쓰라고 알려줍니다. 그 메시지를 저에게 주시면 고쳐드립니다.
- **구글 타겟 API 요구 수준** — 구글은 매년 신규 앱의 타겟 API 수준을 올립니다. 지금은 36으로 맞춰뒀으나, 업로드 시 다른 숫자를 요구하면 그 숫자로 바꾸면 됩니다.
- **조직 계정의 확인 절차** — 국가와 시점에 따라 요구 서류가 다를 수 있습니다. 실제 화면에서 다른 것을 요구하면 알려주십시오.
